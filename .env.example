# Solana RPC URL (use a reliable RPC provider for production)
VITE_SOLANA_RPC_URL=https://api.devnet.solana.com

# Arcium cluster offset for devnet (456 for v0.6.3)
VITE_ARCIUM_CLUSTER_OFFSET=456

# Rule configuration (set after deploying + registering a rule)
# RULE_ID should match the rule registered in the smart contract
VITE_RULE_ID=priority_v1
# RULE_AUTHORITY is the public key that registered the rule
VITE_RULE_AUTHORITY=<your-wallet-public-key>

# Proof epoch - use a fixed epoch for testing, or remove for daily epochs
VITE_EPOCH_SECONDS=86400
# Optional: override epoch for testing (e.g., VITE_EPOCH=1)
# VITE_EPOCH=1

# OAuth credentials (create OAuth apps on each platform)
VITE_TWITTER_CLIENT_ID=<your-twitter-oauth-client-id>
VITE_GITHUB_CLIENT_ID=<your-github-oauth-app-client-id>

# Backend API URL for GitHub token exchange
VITE_API_BASE_URL=http://localhost:8787
